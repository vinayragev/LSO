from django.db import models

# class nav(models.Model) :
#     nav_id   = models.AutoField(primary_key=True)
#     page        = models.CharField(max_length=200, default=None)
#     name        = models.CharField(max_length=200, default=None)
#     active      = models.IntegerField(default=1, choices={0:"Not Active", 1:"Active"})
#     def __str__(self):
#         return self.name
